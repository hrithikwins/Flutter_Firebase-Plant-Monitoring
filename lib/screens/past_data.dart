import "package:flutter/material.dart";

class PastDataPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: SafeArea(
        child: Container(
          child: Text('Past Data History'),
        ),
      ),
    );
  }
}
